spring:
  application:
    name: ms-duenos
  datasource:
    url: jdbc:h2:mem:ms-duenos;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: always
server:
  port: 8082
springdoc:
  swagger-ui:
    path: /swagger-ui.html